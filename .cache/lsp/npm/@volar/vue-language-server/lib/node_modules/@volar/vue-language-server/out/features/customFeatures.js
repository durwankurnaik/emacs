var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.register = void 0;
const shared = require("@volar/shared");
const path = require("upath");
const vscode = require("vscode-languageserver");
const vue = require("@volar/vue-language-core");
const requests_1 = require("../requests");
function register(connection, projects) {
    connection.onRequest(requests_1.D3Request.type, (handler) => __awaiter(this, void 0, void 0, function* () {
        // const document = documents.get(handler.uri);
        // if (!document) return;
        // const languageService = await getLanguageService(document.uri);
        // return languageService?.__internal__.getD3(document);
        return undefined; // disable for now
    }));
    connection.onRequest(requests_1.GetMatchTsConfigRequest.type, (handler) => __awaiter(this, void 0, void 0, function* () {
        var _a;
        return (_a = (yield projects.getProject(handler.uri))) === null || _a === void 0 ? void 0 : _a.tsconfig;
    }));
    connection.onNotification(requests_1.ReloadProjectNotification.type, (handler) => __awaiter(this, void 0, void 0, function* () {
        projects.reloadProject();
    }));
    connection.onNotification(requests_1.WriteVirtualFilesNotification.type, (params) => __awaiter(this, void 0, void 0, function* () {
        var _b;
        const fs = yield Promise.resolve().then(() => require('fs'));
        const project = yield projects.getProject(params.uri);
        if (project) {
            const ls = yield ((_b = (yield project.project)) === null || _b === void 0 ? void 0 : _b.getLanguageServiceDontCreate());
            if (ls) {
                const localTypesFiles = ls.__internal__.vueRuntimeContext.typescriptLanguageServiceHost.getScriptFileNames().filter(fileName => fileName.endsWith(vue.localTypes.typesFileName));
                for (const fileName of localTypesFiles) {
                    const script = ls.__internal__.vueRuntimeContext.typescriptLanguageServiceHost.getScriptSnapshot(fileName);
                    if (script) {
                        fs.writeFile(fileName, script.getText(0, script.getLength()), () => { });
                    }
                }
                const context = yield ls.__internal__.getContext();
                for (const vueDocument of context.vueDocuments.getAll()) {
                    for (const sourceMap of vueDocument.getSourceMaps()) {
                        if (!sourceMap.embeddedFile.isTsHostFile)
                            continue;
                        fs.writeFile(sourceMap.embeddedFile.fileName, sourceMap.mappedDocument.getText(), () => { });
                    }
                }
            }
        }
    }));
    connection.onNotification(requests_1.VerifyAllScriptsNotification.type, (params) => __awaiter(this, void 0, void 0, function* () {
        var _c;
        let errors = 0;
        let warnings = 0;
        const progress = yield connection.window.createWorkDoneProgress();
        progress.begin('Verify', 0, '', true);
        const project = yield projects.getProject(params.uri);
        if (project) {
            const ls = yield ((_c = (yield project.project)) === null || _c === void 0 ? void 0 : _c.getLanguageServiceDontCreate());
            if (ls) {
                const context = yield ls.__internal__.getContext();
                const allVueDocuments = context.vueDocuments.getAll();
                let i = 0;
                for (const vueFile of allVueDocuments) {
                    progress.report(i++ / allVueDocuments.length * 100, path.relative(ls.__internal__.rootPath, shared.getPathOfUri(vueFile.uri)));
                    if (progress.token.isCancellationRequested) {
                        continue;
                    }
                    let _result = yield ls.doValidation(vueFile.uri);
                    connection.sendDiagnostics({ uri: vueFile.uri, diagnostics: _result });
                    errors += _result.filter(error => error.severity === vscode.DiagnosticSeverity.Error).length;
                    warnings += _result.filter(error => error.severity === vscode.DiagnosticSeverity.Warning).length;
                }
            }
        }
        progress.done();
        connection.window.showInformationMessage(`Verification complete. Found ${errors} errors and ${warnings} warnings.`);
    }));
    connection.onRequest(requests_1.DetectDocumentNameCasesRequest.type, (handler) => __awaiter(this, void 0, void 0, function* () {
        const languageService = yield getLanguageService(handler.uri);
        return languageService === null || languageService === void 0 ? void 0 : languageService.__internal__.detectTagNameCase(handler.uri);
    }));
    function getLanguageService(uri) {
        var _a;
        return __awaiter(this, void 0, void 0, function* () {
            const project = (_a = (yield projects.getProject(uri))) === null || _a === void 0 ? void 0 : _a.project;
            return project === null || project === void 0 ? void 0 : project.getLanguageService();
        });
    }
}
exports.register = register;
//# sourceMappingURL=customFeatures.js.map